import subprocess
#cmd = "git --version"

#returned_value = subprocess.call(cmd, shell=True)
#print('returned_values: ', returned_value)
#
# cmd = "date"
# returned_value = subprocess.call(cmd, shell=True)
# print("current date is:", returned_value.decode("utf-8"))
print(dir(subprocess))